<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">
<!-- 
	Mapping file autogenerated by MyEclipse Persistence Tools
-->
<hibernate-mapping>
	<class name="bbr.b2b.regional.logistic.vendors.entities.Ranking" table="RANKING"
		entity-name="bbr.b2b.regional.logistic.vendors.entities.Ranking" schema="LOGISTICA">
		<id name="id" type="java.lang.Long">
			<column name="ID" />
			<generator class="identity">
				<param name="sequence">RANKING_SEQUENCE</param>
				<param name="parameters">AS BIGINT CACHE 100</param>
			</generator>
		</id>
		
		<property name="when" column="when1" type="timestamp"></property>
		<property name="since" type="timestamp"></property>
		<property name="until" type="timestamp"></property>		
			
		<sql-query name="getLatestRanking">
			<![CDATA[	
				SELECT
					R.ID,
					LOGISTICA.TOSTR(R.WHEN1) AS WHEN,
					LOGISTICA.TOSTR(R.SINCE) AS SINCE,
					LOGISTICA.TOSTR(R.UNTIL) AS UNTIL 
				FROM 
					LOGISTICA.RANKING R				
				WHERE 
				 	R.WHEN1 = (SELECT MAX(R1.WHEN1) FROM LOGISTICA.RANKING R1) 
			]]>
		</sql-query>
		
		<sql-query name="getRankingById">
			<![CDATA[	
				SELECT
					R.ID,
					LOGISTICA.TOSTR(R.WHEN1) AS WHEN,
					LOGISTICA.TOSTR(R.SINCE) AS SINCE,
					LOGISTICA.TOSTR(R.UNTIL) AS UNTIL
				FROM 
					LOGISTICA.RANKING R				
				WHERE 
				 	R.ID = :rankingid
			]]>
		</sql-query>				
	</class>	
</hibernate-mapping>




